{
  "buildCommand": "echo Using committed Flutter web build",
  "outputDirectory": "flutter_app/build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
